import { MultiSearchPipe } from './multi-search.pipe';

describe('MultiSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
